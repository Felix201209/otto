# Otto v1.7.3

这是内部测试的 Lark CLI 性能补丁：

- 复用 npm 缓存中经过包名与固定版本校验的 Lark CLI 原生二进制。
- 热启动实测由 2.51 秒降至 0.38 秒，约减少 85% 启动开销。
- 首次仍保留 `npx` 零安装路径；首次安装后自动走原生缓存。

## 安装提示

macOS 版本采用本地完整性校验与 ad-hoc 签名，尚未完成 Apple 公证。首次打开如被系统拦截，请在“系统设置 → 隐私与安全性”中允许打开。Windows 版本尚未使用商业代码签名，SmartScreen 可能提示未知发布者。
